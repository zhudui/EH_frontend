import Mock from 'mockjs'
// import auth from './auth'

// 登录相关
// Mock.mock('/api/user/login', 'post', auth.loginByUsername);
// Mock.mock('/api/user/userInfo', 'post', auth.getInfo);
// Mock.mock('/api/user/logout', 'post', auth.logout);

export default Mock
